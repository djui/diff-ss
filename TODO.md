# Make CFG_* program argument
# Read colors to be used from git config
# Mark trailing whitespace (if wanted)
# Convert to state machine
# Read line by line instead of getting everything up front
# Minimize buffer
# Write tests
